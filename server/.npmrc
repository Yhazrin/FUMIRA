registry=https://registry.npmjs.org/
# Local project HTTP proxy (port 7990). Used for MiniMax outbound calls.
# Comment out for npm install when the proxy is not running.
proxy=http://127.0.0.1:7990
https-proxy=http://127.0.0.1:7990
