registry=https://registry.npmjs.org/
# Keep machine-local proxies out of the tracked npm configuration.
# Developers who require the local 7990 proxy should export HTTP_PROXY and
# HTTPS_PROXY in their shell or use an untracked user-level ~/.npmrc.
