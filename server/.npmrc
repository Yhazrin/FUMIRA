registry=https://registry.npmjs.org/
# Configure local proxies through environment variables instead of committing
# machine-specific localhost endpoints. GitHub Actions and other developers
# cannot reach a proxy running only on one workstation.
