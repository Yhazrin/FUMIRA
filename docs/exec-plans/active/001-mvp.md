# Execution Plan 001 — Mature Mock MVP

Status: completed on 2026-07-23.

## Goal

Deliver a buildable, visually faithful SwiftUI mock MVP with the requested
continuous ±100-year nonlinear time control.

## Stages

1. Codex freezes architecture, protocols, state machine, design tokens, and tests.
2. Cursor implements only DesignSystem components/motion and Feature screens.
3. Codex inspects the diff, integrates, builds, tests, launches, and captures evidence.
4. Codex fixes acceptance gaps and records any deferred real-service risk.

## Cursor write scope

- `FUMIRA/DesignSystem/Components/**`
- `FUMIRA/DesignSystem/Motion/**`
- `FUMIRA/Features/**`
- `FUMIRA/PreviewSupport/**`

Cursor may read everything but must not modify App, Domain, Services, project
configuration, architecture documents, or public protocols.

## Done

All checks in `docs/engineering/ACCEPTANCE_TESTS.md` pass, simulator evidence is
inspected, and the working tree contains no unexplained files.

## Verification record

- XcodeGen project generation and clean iOS Simulator build succeeded.
- The original seven unit tests passed with zero failures; Plan 002 expanded the
  current suite to twelve.
- iPhone 17 Pro runtime flow reached connection, permissions, viewfinder,
  generation, result, poster, and generation-failure recovery states.
- Continuous scrubbing was observed from day-level precision near NOW into a
  multi-decade future value without snapping.
- Bundled fonts and the asset catalog were verified inside the built app; the
  post-fix runtime log contains no font-loading errors.
- Cursor's out-of-scope regeneration changed only `project.pbxproj`; Codex then
  regenerated the project from the reviewed `project.yml` during integration.
